# 问题与建议

## 问题

1. 必须要讲小模型相关的内容
2. 必须把课后作业的题目确定, 这样才可以制作课件与设计大纲和内容.
3. 课程的作业代码已经提供了一部分, 然后只需要修改其中的部分代码就可以了, 我这边就是需要把课上的部分拷贝到服务端实现的部分就可以了. 稍微修改一下代码就可以了.
4. 将我的 Jupyter Notebook 打包成一个压缩包, 包括数据与 Jupyter Notebook
5. 上传的 PDF 带背景图就可以了, 讲课的时候不涉及.

## TODO

1. 课程的作业代码, 主要是服务端的代码部分

## 实现的难点

1. 小模型无法运行, 因为当前容器资源有限, 没有显卡, 作业无法布置.
2. 小模型需要微调, 还得部署, 文本的处理方式, 特征提取.
3. 前面的需要砍一些.
4. 课程代练的过程是带领大家离线部署一个小模型.
5. 扩展微调或者自己训练模型的方式.

## 修改建议

现在的课程是从数据分析, Numpy, Pandas, 到小文本模型的训练与使用, 小文本模型选择使用 BERT Small 模型.  

1. 数据分析部分太多了, Numpy 和 Pandas 的内容太多了, 需要删减一部分, 数据分析扩展这部分可以完全删除掉
2. 不需要讲的太难, 需要讲的更加实用, 从实用的脚本解释与讲解.
3. 现在的 Q1-Q7, 一共 7 个问题, 太多了, 只需要着重解释前面 5 个问题就可以了.
4. 需要讲一下文本处理与文本分析, 以及给出实例说明
5. 需要从, 样本划分 (Data Partitioning), 损失计算 (Loss Calculation / 前向传播), 梯度计算 (Gradient Calculation / 反向传播), 参数更新 (Parameter Update) 这四个点讲一下模型微调, 需要注意学员有些是小白, 因此需要用大白话解释, 然后让学员理解这个流程就可以了, 不需要讲公式, 然后这些流程都需要给出一个 Python 的实现, 就是对应在 Python中的实现. 
6. 我们最终的课后作业是一个意图识别的场景, 输入是用户的问题, 然后分类为不同的意图, 因此上面的实例也使用这个例子和数据集, 需要微调 
BERT Small 模型, 现场我会给出一个小小的数据集测试.
7. 最后讲一下模型的部署, 需要讲一下如何最小化的部署微调之后的模型, 部署是在干什么, 这里需要稍微讲一下原理, 使用 ONNX Runtime 作为示例讲解. 


## 云闪付评测的逻辑

1. 评测指标, 评测的 Prompted
2. 具体分数是多少, 验证的逻辑是达到多少分, 还是比基模好. 
3. 下次测试的时候, 需要将历史的评测集都测试一下, 整理全都测试完验收

